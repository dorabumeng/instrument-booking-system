"use client";
import Link from "next/link"; import { LabMark } from "./icons"; import { signOut } from "@/lib/auth/actions"; import { LanguageSwitch, useI18n } from "@/lib/i18n/client"; import type { Profile } from "@/types/database";
export default function Navbar({ profile }: { profile: Profile }) { const { t } = useI18n(); const links = [[t("nav.dashboard"), "/dashboard"], [t("nav.instruments"), "/instruments"], [t("nav.bookings"), "/bookings"]] as const; return <header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-3 sm:px-6 lg:px-8"><Link href="/dashboard" className="flex min-w-0 items-center gap-2 font-bold"><LabMark /><span className="hidden truncate lg:inline">{t("brand.short")}</span></Link><nav aria-label="Primary navigation" className="hidden items-center gap-1 md:flex">{links.map(([label, href]) => <Link key={href} href={href} className="rounded-lg px-3 py-2 text-sm font-semibold text-slate-600 hover:bg-slate-100">{label}</Link>)}{profile.role === "admin" && <Link href="/admin" className="rounded-lg px-3 py-2 text-sm font-semibold text-slate-600">{t("nav.admin")}</Link>}</nav><div className="flex items-center gap-2"><LanguageSwitch /><Link href="/profile" title={t("nav.profile")} className="grid size-9 place-items-center rounded-full bg-teal-100 text-sm font-bold text-teal-800">{profile.full_name.slice(0,2).toUpperCase()}</Link><form action={signOut}><button className="hidden rounded-lg px-3 py-2 text-sm font-bold text-slate-500 sm:block">{t("nav.logout")}</button></form></div></div><nav aria-label="Mobile navigation" className="flex justify-around border-t border-slate-100 px-2 py-1 md:hidden">{links.map(([label, href]) => <Link key={href} href={href} className="px-2 py-2 text-xs font-semibold">{label}</Link>)}{profile.role === "admin" && <Link href="/admin" className="px-2 py-2 text-xs font-semibold">{t("nav.admin")}</Link>}</nav></header>; }
