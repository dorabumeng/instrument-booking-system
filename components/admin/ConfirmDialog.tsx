"use client";
import { useEffect, useRef } from "react";
export default function ConfirmDialog({ title, message, confirmLabel, dangerous = false, pending = false, onConfirm, onClose }: { title: string; message: string; confirmLabel: string; dangerous?: boolean; pending?: boolean; onConfirm: () => void; onClose: () => void }) { const ref = useRef<HTMLButtonElement>(null); useEffect(() => { ref.current?.focus(); const listener = (event: KeyboardEvent) => { if (event.key === "Escape" && !pending) onClose(); }; window.addEventListener("keydown", listener); return () => window.removeEventListener("keydown", listener); }, [onClose, pending]); return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-5" role="presentation"><section role="alertdialog" aria-modal="true" aria-labelledby="confirm-title" aria-describedby="confirm-message" className="card w-full max-w-md p-6"><h2 id="confirm-title" className="text-xl font-bold">{title}</h2><p id="confirm-message" className="mt-3 leading-6 text-slate-600">{message}</p><div className="mt-6 flex justify-end gap-3"><button disabled={pending} onClick={onClose} className="btn-secondary">Keep unchanged</button><button ref={ref} disabled={pending} onClick={onConfirm} className={dangerous ? "rounded-xl bg-red-700 px-4 py-3 font-bold text-white disabled:opacity-50" : "btn-primary disabled:opacity-50"}>{pending ? "Working…" : confirmLabel}</button></div></section></div>; }
