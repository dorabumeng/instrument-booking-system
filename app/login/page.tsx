import { LabMark } from "@/components/icons"; import AuthForm from "@/components/AuthForm"; import { LanguageSwitch } from "@/lib/i18n/client"; import { getDictionary } from "@/lib/i18n/server"; export const dynamic = "force-dynamic";
export default async function LoginPage() { const { t } = await getDictionary(); return <main className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[#103d3b] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3 text-lg font-bold"><LabMark /> {t("brand.full")}</div><div className="max-w-lg"><h1 className="text-5xl font-bold leading-tight">{t("brand.full")}</h1><p className="mt-5 text-lg text-teal-50/80">Secure instrument scheduling for laboratory research.</p></div><p className="text-sm text-teal-100/60">Southern University of Science and Technology</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><div className="mb-6 flex justify-end"><LanguageSwitch /></div><p className="eyebrow">{t("login.access")}</p><h2 className="mt-3 text-3xl font-bold">{t("login.welcome")}</h2><p className="mb-8 mt-2 text-slate-500">{t("login.help")}</p><AuthForm /></div></section></main>; }
