"use client"; import Link from "next/link"; import { useI18n } from "@/lib/i18n/client";
export default function AdminNav() { const { t } = useI18n(); const links = [[t("admin.dashboard"), "/admin"], [t("admin.instruments"), "/admin/instruments"], [t("admin.bookings"), "/admin/bookings"], [t("admin.users"), "/admin/users"], [t("admin.audit"), "/admin/audit"]]; return <nav aria-label="管理后台导航" className="mb-8 flex gap-2 overflow-x-auto rounded-xl border border-slate-200 bg-white p-2">{links.map(([label, href]) => <Link key={href} href={href} className="whitespace-nowrap rounded-lg px-4 py-2 text-sm font-bold text-slate-600 hover:bg-teal-50">{label}</Link>)}</nav>; }
