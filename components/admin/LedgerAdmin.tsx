"use client";
import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { saveLedgerDetails } from "@/lib/admin/ledger-mutations";
import { formatAdminDate, ledgerValueLabels } from "@/lib/admin/labels";
import type { AdminBooking } from "@/types/booking";
import type { LedgerInput } from "@/types/ledger";
export default function LedgerAdmin({ bookings }: { bookings: AdminBooking[] }) { const [selected, setSelected] = useState<AdminBooking>(); return <section className="card mt-6 p-5 sm:p-6"><div><p className="eyebrow">学校台账</p><h2 className="mt-1 text-xl font-bold">管理台账详情</h2><p className="mt-2 text-sm text-slate-500">预约完成后可补充财务与合规信息。</p></div><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{bookings.map(item => <button key={item.id} onClick={() => setSelected(item)} className="rounded-xl border border-slate-200 p-3 text-left hover:border-teal-500"><strong>{item.instrument.name} · {item.user.full_name}</strong><span className="mt-1 block text-xs text-slate-500">{formatAdminDate(item.start_time)} · {item.ledger ? "已填写台账" : "待补充台账"}</span></button>)}</div>{selected && <LedgerDialog booking={selected} onClose={() => setSelected(undefined)} />}</section>; }
function LedgerDialog({ booking, onClose }: { booking: AdminBooking; onClose: () => void }) { const router = useRouter(); const [pending, startTransition] = useTransition(); const d = booking.ledger; const [value, setValue] = useState<LedgerInput>({ sample_count: d?.sample_count ?? 1, statistical_hours: d?.statistical_hours ?? null, payer_name: d?.payer_name ?? "", payer_organization: d?.payer_organization ?? "", billing_status: d?.billing_status ?? "pending", contract_status: d?.contract_status ?? "not_required", contract_amount: d?.contract_amount ?? null, evaluation_status: d?.evaluation_status ?? "not_required", survey_status: d?.survey_status ?? "not_required" }); const [error, setError] = useState(""); const set = (key: keyof LedgerInput, field: string | number | null) => setValue(current => ({ ...current, [key]: field })); return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4"><section role="dialog" aria-modal="true" aria-labelledby="ledger-title" className="max-h-[95vh] w-full max-w-2xl overflow-y-auto rounded-2xl bg-white p-6"><h2 id="ledger-title" className="text-xl font-bold">学校台账详情</h2><p className="mt-1 text-sm text-slate-500">{booking.instrument.name} · {booking.user.full_name}</p><form className="mt-5 grid gap-4 sm:grid-cols-2" onSubmit={event => { event.preventDefault(); startTransition(async () => { const result = await saveLedgerDetails(booking.id, value); if (result.success) { router.refresh(); onClose(); } else setError(result.message); }); }}><NumberField label="样品数量" value={value.sample_count ?? 1} onChange={v => set("sample_count", v)} /><NumberField label="统计时长（可选）" value={value.statistical_hours ?? ""} step="0.001" onChange={v => set("statistical_hours", v === "" ? null : Number(v))} /><TextField label="付款方" value={value.payer_name ?? ""} onChange={v => set("payer_name", v)} /><TextField label="付款单位" value={value.payer_organization ?? ""} onChange={v => set("payer_organization", v)} /><SelectField label="计费状态" value={value.billing_status ?? "pending"} options={["pending","charged","exempt","not_applicable"]} onChange={v => set("billing_status", v)} /><SelectField label="合同状态" value={value.contract_status ?? "not_required"} options={["signed","not_signed","not_required"]} onChange={v => set("contract_status", v)} /><NumberField label="合同金额（CNY）" value={value.contract_amount ?? ""} step="0.01" onChange={v => set("contract_amount", v === "" ? null : Number(v))} /><SelectField label="评价状态" value={value.evaluation_status ?? "not_required"} options={["submitted","not_submitted","not_required"]} onChange={v => set("evaluation_status", v)} /><SelectField label="问卷状态" value={value.survey_status ?? "not_required"} options={["completed","not_completed","not_required"]} onChange={v => set("survey_status", v)} />{error && <p className="sm:col-span-2 text-sm text-red-700" role="alert">{error}</p>}<div className="flex justify-end gap-3 sm:col-span-2"><button type="button" onClick={onClose} className="btn-secondary">取消</button><button disabled={pending} className="btn-primary">{pending ? "保存中……" : "保存台账"}</button></div></form></section></div>; }
function TextField({ label, value, onChange }: { label: string; value: string; onChange: (v: string) => void }) { return <label className="text-sm font-bold">{label}<input value={value} maxLength={300} onChange={e => onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal" /></label>; }
function NumberField({ label, value, step = "1", onChange }: { label: string; value: string | number; step?: string; onChange: (v: string | number) => void }) { return <label className="text-sm font-bold">{label}<input type="number" min="0" step={step} value={value} onChange={e => onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal" /></label>; }
function SelectField({ label, value, options, onChange }: { label: string; value: string; options: string[]; onChange: (v: string) => void }) { return <label className="text-sm font-bold">{label}<select value={value} onChange={e => onChange(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 p-3 font-normal">{options.map(item => <option key={item} value={item}>{ledgerValueLabels[item] ?? item}</option>)}</select></label>; }
