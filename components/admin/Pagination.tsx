import Link from "next/link";
export default function Pagination({ page, pageCount, total, params }: { page: number; pageCount: number; total: number; params: Record<string, string | undefined> }) { const href = (next: number) => { const query = new URLSearchParams(); Object.entries(params).forEach(([key, value]) => { if (value && key !== "page") query.set(key, value); }); query.set("page", String(next)); return `?${query.toString()}`; }; return <nav aria-label="Pagination" className="mt-6 flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 pt-5"><p className="text-sm text-slate-500">{total} record{total === 1 ? "" : "s"} · Page {page} of {pageCount}</p><div className="flex gap-2"><Link aria-disabled={page <= 1} className={`btn-secondary ${page <= 1 ? "pointer-events-none opacity-40" : ""}`} href={href(Math.max(1, page - 1))}>Previous</Link><Link aria-disabled={page >= pageCount} className={`btn-secondary ${page >= pageCount ? "pointer-events-none opacity-40" : ""}`} href={href(Math.min(pageCount, page + 1))}>Next</Link></div></nav>; }
