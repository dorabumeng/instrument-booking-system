import PageIntro from "@/components/PageIntro";
import Link from "next/link";
import { getAdminDashboard } from "@/lib/admin/dashboard";
export default async function AdminPage() { const result = await getAdminDashboard(); const stats = result.data; const cards = stats ? [["Total instruments", stats.totalInstruments], ["Available", stats.availableInstruments], ["Maintenance", stats.maintenanceInstruments], ["Laboratory users", stats.totalUsers], ["Upcoming bookings", stats.upcomingBookings], ["Today’s bookings", stats.todaysBookings]] : []; const modules = [["Instruments", "/admin/instruments", "Manage equipment, status, maintenance and archival."], ["Bookings", "/admin/bookings", "Inspect, edit, cancel and export reservations."], ["Users", "/admin/users", "Search members and manage administrator roles."], ["Audit Log", "/admin/audit", "Review protected operational activity."]]; return <><PageIntro eyebrow="Restricted area" title="Laboratory administration" description="Operational controls backed by server authorization, Row Level Security, and database audit trails." />{result.error ? <div className="card p-6 text-red-700" role="alert">{result.error}</div> : <><section aria-label="Administration summary" className="mb-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label, value]) => <article key={label} className="card p-5"><p className="text-sm font-semibold text-slate-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></article>)}</section><section className="grid gap-5 md:grid-cols-2">{modules.map(([title, href, text]) => <Link key={href} href={href} className="card p-6 transition hover:-translate-y-0.5 hover:border-teal-300"><p className="eyebrow">Manage</p><h2 className="mt-2 text-xl font-bold">{title}</h2><p className="mt-2 leading-6 text-slate-600">{text}</p><span className="mt-5 inline-block font-bold text-teal-700">Open {title} →</span></Link>)}</section></>}</>; }
