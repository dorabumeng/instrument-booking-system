"use client";
import { useMemo, useState } from "react";
import InstrumentCard from "./InstrumentCard";
import type { Instrument, InstrumentStatus } from "@/types/instrument";
export default function InstrumentGrid({ instruments }: { instruments: Instrument[] }) { const [search, setSearch] = useState(""); const [status, setStatus] = useState<"all" | InstrumentStatus>("all"); const filtered = useMemo(() => instruments.filter(item => (status === "all" || item.status === status) && `${item.name} ${item.location} ${item.manager}`.toLowerCase().includes(search.toLowerCase())), [instruments, search, status]); return <><div className="mb-6 flex flex-col gap-3 sm:flex-row"><input value={search} onChange={e => setSearch(e.target.value)} aria-label="Search instruments" placeholder="Search instruments, locations, or managers" className="w-full rounded-xl border border-slate-200 bg-white px-4 py-3 sm:max-w-md" /><select value={status} onChange={e => setStatus(e.target.value as "all" | InstrumentStatus)} aria-label="Filter by status" className="rounded-xl border border-slate-200 bg-white px-4 py-3"><option value="all">All statuses</option><option value="available">Available</option><option value="maintenance">Maintenance</option><option value="unavailable">Unavailable</option></select></div>{filtered.length ? <div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(item => <InstrumentCard key={item.id} instrument={item} />)}</div> : <div className="card p-10 text-center"><h2 className="text-lg font-bold">No matching instruments</h2><p className="mt-2 text-sm text-slate-500">Try changing the search or status filter.</p></div>}</>; }
